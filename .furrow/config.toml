# furrow — board configuration (.furrow/config.toml).
# https://github.com/akira-toriyama/furrow
#
# Ordinary furrow commands only READ this file; the one writer is
# `furrow config set <key> <value>`, which edits a single key surgically and
# preserves comments. Unknown keys and out-of-range values are clamped to safe
# defaults on read (a typo can't break the tool); `furrow lint` reports what it clamped, unknown keys with their line.
#
# Two top-level keys (bare — TOML requires them ABOVE the [sections]):
#
# The board's MODE: "shared" (a git remote and co-writers — the default) or
# "standalone" (one machine, its own git, no remote, no `furrow sync`, no CI).
# It changes only wording (`furrow upgrade` drops the shared-board flag-day
# checklist) — never behavior, the schema gate, or an on-disk byte. The OTHER
# axis, layout (central / repo-local), is not configured: it follows from how
# discovery reached this board. `furrow board` prints both.
# mode = "shared"
#
# The repo this board is FOR. `furrow add` attaches it and reads filter by it
# whenever discovery supplied no scope of its own — i.e. from inside the board's
# own directory tree, or under FURROW_DIR. A pointer's default_repo and a
# user-level [[board]]'s repo are nearer and still win. A literal "owner/repo"
# only: this file is committed and shared, so "auto" is refused (a derived repo
# would differ per checkout, which is the cwd-dependence this key removes).
# default_repo = "owner/repo"
default_repo = "akira-toriyama/ridge-test"

[lanes]
# The status enum AND the top->bottom sort rank. Mirrors GitHub Projects #5.
# Editing this list is how you rename or reorder statuses. A task whose status
# is not in this list sorts last and is flagged by `furrow lint`.
# "waiting" is the GTD Waiting-For lane (delegated / blocked on someone else).
order = ["inbox", "backlog", "in-progress", "ready", "waiting", "done", "icebox"]
# Lane assigned by `furrow add` when --status is omitted.
default = "inbox"
# Lane `furrow done` moves a task into (and where Closed is stamped).
done = "done"
# Lanes whose tasks are NOT actionable for `furrow next` (done, parked,
# and waiting-for — the last two are parked, so they never stamp Closed).
terminal = ["done", "icebox", "waiting"]

[next]
# Lanes `furrow next` considers "ready to work" (besides the deps-done
# check). Intake/planning lanes are excluded so next stays focused. Set to all
# non-terminal lanes if you want next to show everything actionable.
lanes = ["in-progress", "ready"]

[priority]
# Sparse integer step so reordering edits one field instead of renumbering.
step = 10
default = 100

[ids]
# Random id format: prefix + a random Crockford-base32 suffix (e.g. t-k3m9p).
# Ids are frozen (never reused); randomness avoids collisions when several
# operators add tasks concurrently. width = number of random suffix chars.
prefix = "t-"
width = 5 # t-k3m9p
# Epics get their OWN prefix so an id names its entity kind on sight
# (e.g. e-k3m9p). Must differ from prefix (furrow falls back to "e-" if not).
epic_prefix = "e-"

[labels]
# When true, every task must carry at least one label — `furrow add` rejects a
# label-less task and `furrow lint` flags one. Handy when labels mean something
# mandatory (e.g. a central tracker where the label is the owning repo).
# required = false

[archive]
# Default window for `furrow archive --older-than` (days; done tasks only).
older_than_days = 30

[lint]
# `furrow lint` warns when at least this many done tasks are older than
# [archive].older_than_days and ready to archive — a nudge to run `furrow archive`.
# 0 (the default) disables the nudge.
# archive_done = 0
# Lint codes to suppress on every run (a permanently-dead check that keeps firing).
# An entry naming no real code only warns (clamp-don't-reject). Filtering drives the
# exit code, so ignoring the last error makes lint exit 0. CLI twins: `--code` /
# `--exclude-code` / `--severity error` (those reject an unknown code with exit 2).
# ignore_codes = ["reconcile-gap", "epic-no-active"]
# When set, `furrow lint` warns `provenance-missing` on every open, non-terminal
# task whose body contains NONE of these substrings (case-insensitive) — the
# "where did this come from / how was it verified" pressure, in the board's own
# words. Empty (the default) keeps the check off; the vocabulary is the board's
# convention, not furrow's.
# provenance_markers = ["Source:", "Verified:"]
# When set, `furrow lint` warns `title-scope-marker` on every open, non-terminal
# task whose TITLE contains one of these substrings (case-insensitive) — the
# board's idiom for "scope narrowed in place", the signal to split the remainder
# out or retire the task instead of annotating further. Empty (default) = off.
# title_scope_markers = ["残り:", "only:"]
# When > 0, `furrow lint` warns `stale-inbox` on every open task sitting in the
# default (intake) lane for at least this many days without an update — the
# inbox-zero pressure (intake is for triage; backlog is where waiting is fine).
# Shorter and lane-bound, unlike [revisit].stale_days. 0 (the default) = off.
# stale_inbox_days = 0

# [lint.severity] re-levels one code for THIS board: code = "error" | "warn".
# For the code whose shipped level fits one operation and not another —
# `due-overdue` is an error because a shared board's CI gate consumes the red,
# so a standalone board (no CI) demotes it to a warn in one committed line;
# promoting a warn it wants gating works the same way. Unlike ignore_codes the
# finding stays VISIBLE — only its level (and with it lint's exit code and the
# sync ride-along's error count) changes. A level other than error/warn is
# clamped away with a warning; a code naming no real check warns via `furrow lint`.
# [lint.severity]
# due-overdue = "warn"
provenance_markers = ["完了条件:"]

[due]
# Lanes where a due date raises NOTHING — no lint finding, no `furrow brief`
# section entry. The done lane is always exempt on top of this (a closed task's
# promise is settled). Terminal lanes are deliberately NOT exempt as a class:
# "waiting" is terminal here, and a waiting task whose only remaining step is
# "look at this on that day" is exactly what a due date is for.
# ignore_lanes = ["icebox"]

# The calendar this board keeps. An IANA zone name; unset means the zone of
# whatever process runs the command. Declared here because config.toml is
# committed and shared: a bare --due date binds the END of that day, and "that
# day" has to mean one thing whether the operator's machine or a UTC CI runner
# writes it. A name that will not load falls back to the process zone with a
# warning that furrow lint surfaces. A shared board that carries a REPEATING
# task needs one: its series is expanded, and the day a close settles is
# chosen, in the zone of whichever machine closes it, so a UTC CI runner lands
# a weekly/monthly occurrence a day off for anyone bound in another zone —
# furrow lint errors repeat-no-timezone until the calendar is declared.
# timezone = "Asia/Tokyo"
timezone = "Asia/Tokyo"

[revisit]
# Days a task may go without an update before `furrow revisit` flags it
# stale. 0 disables the stale signal (the other revisit signals still fire).
stale_days = 30

[review]
# Days a repo may go without a human `furrow review <repo>` before
# `furrow sync` nudges it as unreviewed (on the revisit line), AND days a
# STANDING epic may go past its last `furrow review <epic-ref>` before
# revisit raises epic_review_due — one review clock for the whole board.
# 0 disables both. A never-reviewed repo or box stays quiet either way.
stale_after_days = 14

[session]
# The co-located-session write guard, active only under Claude Code (the
# CLAUDECODE env; a human shell and CI pass untouched). A write that touches a
# repo another, EARLIER-started session on this machine sits in is refused
# (exit 2, kind `session-busy`; `add` escapes with --draft) while that session
# is working, and goes through with a warning (stderr + `session_warn` in the
# --json envelope) once it is idle. Idle = that session's transcript ends in an
# assistant end_turn record (its turn is over, however recent the write), or,
# when the turn is not known to have ended, it has been silent for more than
# this many seconds — the ceiling that keeps a long tool call or an unanswered
# permission prompt from refusing forever. A session whose transcript cannot
# be found is measured from its start under the same ceiling. 0 turns refusals
# off: every clash is a warning (the guard stays on).
busy_seconds = 300

[alias]
# Board-level command aliases, git-style: `furrow <name> …` expands to the
# command string with the rest of argv appended. A builtin always wins (a
# shadowing alias is inert; `furrow lint` warns `alias-shadow`). Lives in this
# committed file so the whole board shares one vocabulary.
# triage = "set --status backlog"
